import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cl.frmarch.gastoscomp',
  appName: 'AppGastosCompartidos',
  webDir: 'www'
};

export default config;
